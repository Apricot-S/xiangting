[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
publish = false

[lints]
workspace = true

[features]
build-map = ["dep:xiangting"]

[dependencies]
xiangting = { path = "..", features = ["build-map"], optional = true }
